(defmacro defn
  [name args & body]
  `(def ~name (λ ~args ~@body)))

(defmacro unless
  [pred a b]
  `(if ~pred ~b ~a))

(defn not
  [a]
  (if a false true))

(defn load-file
  [f]
  (eval (read-string (str "(do " (slurp f) ")"))))
